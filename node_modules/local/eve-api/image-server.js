'use strict';


function get(id, type, size) {
    var f = type === "character" ? ".jpg" : ".png";

    return "https://image.eveonline.com/" + type + "/" + id + "_" + size + f;
}

module.exports = get;