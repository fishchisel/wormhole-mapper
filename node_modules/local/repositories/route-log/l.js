'use strict';

var base = require('../base');

var q = require('q');


module.exports = L;

var path = 'r-l';

var _ref;
function ref() {
    if (!_ref) _ref = base.getRef().child(path);
    return _ref;
}


function L(userId, startSysId, endSysId) {
    if (!userId) userId = "noid";
    if (!startSysId) startSysId = null;
    if (!endSysId) endSysId = null;
    var now = Date.now();

    ref().push({
        uid: userId,
        time: now,
        ssid: startSysId,
        esid: endSysId
    })
}